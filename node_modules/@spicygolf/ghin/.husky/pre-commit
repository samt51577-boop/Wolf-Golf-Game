#!/bin/sh
# Pre-commit hook that runs the same quality checks as CI

set -e

echo "🎨 Running code quality checks..."

# Check code quality with biome
echo "  → Running biome check..."
bun biome check ./src

# Run linting (biome lint + tsc)
echo "  → Running lint..."
bun run lint

# Build the project
echo "  → Running build..."
bun run build

echo "✅ All checks passed!"
